keyboard_navigation = (key) ->
  switch key.which
    when 78 then window.location.replace($('#next_subsequent_episode').val())
    when 80 then window.location.replace($('#next_previous_episode').val())
    else return
  key.preventDefault()

jQuery -> $(document).bind 'keydown', (key) => keyboard_navigation(key)
