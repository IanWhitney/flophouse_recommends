keyboard_navigation = (key) ->
  switch key.which
    when 37 then window.location.replace($('#next_subsequent_episode').val())
    when 39 then window.location.replace($('#next_previous_episode').val())
    else return
  key.preventDefault()

jQuery -> $(document).bind 'keydown', (key) => keyboard_navigation(key)